name: Tests
on:
  pull_request:
    branches:
     - 'main'
jobs:
  cypress-run:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install modules
        run: npm install
      - name: Run unit tests
        run: npm run test:unit
